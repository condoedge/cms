<?php

// Empty stub — this file was removed in cms HEAD but the parent app's
// composer.lock still pins v0.6.67 which expects it in autoload_files.
// Stub keeps the autoloader happy until the lock file is bumped to a
// release that drops the autoload entry.
